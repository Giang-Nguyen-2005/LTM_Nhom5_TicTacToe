Optional assets for the pygame client.

- font.ttf : optional TTF font. If missing, client falls back to system font.
- click.wav : optional click sound. If missing, client continues without sound.

Place any desired font or wav file into this directory. Do not commit large binaries to git if not necessary.
